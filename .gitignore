# music archive content (FLAC archive + ALAC inbox) – not tracked in git
/cd/
/covers/

# configuration with passwords and allowed IPs
/config.json

# prepared album downloads (one ZIP per user, see downloadLib.php)
/tmp/
